<% if @facade.recipe_instruction.destroyed? %>
  <%= turbo_stream_action_tag(
    "remove",
    target: dom_id(@facade.recipe_instruction)
  ) %>
<% end %>

<%= turbo_stream.update :flash do %>
  <%= render "application/flash", flash: flash %>
<% end %>

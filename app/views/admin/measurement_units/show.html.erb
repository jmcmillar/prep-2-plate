<%= render HeaderBarComponent.new(title: "Admin") do |c| %>
  <% c.with_breadcrumb_collection(@facade.breadcrumb_trail) %>
  <% c.with_icon_link_collection(@facade.header_actions)%>
<% end %>
<%= render ContainerComponent.new(class: "mb-10") do %>
  <%= render SectionCardComponent.new(title: @facade.measurement_unit.name) do |card| %>
  <% end %>
<% end %>
<%= turbo_frame_tag :measurement_unit_aliases, src: admin_measurement_unit_measurement_unit_aliases_path(@facade.measurement_unit) %>

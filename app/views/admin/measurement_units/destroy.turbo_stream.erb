<% if @facade.measurement_unit.destroyed? %>
  <%= turbo_stream_action_tag(
    "remove",
    target: dom_id(@facade.measurement_unit)
  ) %>
<% end %>

<%= turbo_stream.update :flash do %>
  <%= render "application/flash", flash: flash %>
<% end %>

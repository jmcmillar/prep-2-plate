<% if @facade.ingredient_category.destroyed? %>
  <%= turbo_stream_action_tag(
    "remove",
    target: dom_id(@facade.ingredient_category)
  ) %>
<% end %>

<%= turbo_stream.update :flash do %>
  <%= render "application/flash", flash: flash %>
<% end %>
